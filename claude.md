# プロジェクト概要と開発ガイドライン

## 1. プロジェクトの目的
本プロジェクトの目的は、フルHTMLで構築されたWebサイトにおいて、データベース（MySQL等）を使用せず、**JSON＋PHPによる軽量かつ保守性の高い「お知らせ／NEWS更新システム（簡易CMS）」**を構築することです。

### 解決する課題
- 1行程度の日付・テキスト・リンクからなる「お知らせ」セクションを、専門知識のないクライアントでも専用の管理画面から直感的に追加・編集・削除できるようにする。
- データベース接続を不要にすることで、サーバー引っ越し・バックアップ作業（FTPでのファイル一括操作）を簡略化し、サイトの表示速度とセキュリティを向上させる。

---

## 2. コード編集範囲およびファイル操作権限
- **編集許可範囲**: 指示されたHTMLファイル、ならびに関連するCSS、JavaScript、PHP、JSON、その他プロジェクト配下の構成ファイル全般。
- **自律的修正**: 指示された機能追加や修正に伴い、整合性を保つために必要と判断される箇所（共通CSSの調整、JSのバインド設定、PHPハンドラーの追加など）については、事前の個別の許可を求めることなく直接編集・作成を行ってよいものとします。

---

## 3. 動作確認および報告手順の省略ルール
- **ブラウザ動作確認の不要**: 指示のたびに開発サーバー（dev server）を起動してのUI確認や、Puppeteer / Playwright等を用いたスクリーンショットの撮影・確認手順は**不要**とします。
- **完了報告スタイル**: コードの書き換え・新規作成・整合性のチェックが完了した時点で、動作確認プロセスを挟むことなく、即座に「コード作成・修正完了」としてユーザーへ報告してください。

## 4. JSON管理実装のベストプラクティス・セキュリティ仕様

本プロジェクトでJSON＋PHP構成を実装するにあたり、セキュリティ事故やデータ破損を防ぐため、以下の仕様・設計方針を必須で適用・遵守してください。

### ① 排他制御（ファイルロック）の実施
管理画面からの更新時に同時書き込みが発生し、JSONファイルが破損（空ファイル化）するのを防ぐため、ファイル操作時は必ず排他ロックをかけてください。

* **書き込み処理**: `file_put_contents('data/news.json', $json_data, LOCK_EX);` のように、必ず `LOCK_EX` フラグを指定する。
* **読み込み処理**: 必要に応じて `fopen` / `flock($fp, LOCK_SH)` を使用し、書き込み中の不完全なデータの読み込みを防止する。

---

### ② JSONファイルの直接アクセス保護・ディレクトリ設計
`news.json` などのデータファイルが外部からURL直接入力で閲覧・操作・削除されないよう、アクセス制御を徹底してください。

* **アクセス制限設定**: データ保存ディレクトリ（例: `/data/`）直下に `.htaccess` を配置し、Webからの直接アクセスを遮断する。
  ```apache
  # /data/.htaccess
  Require all denied